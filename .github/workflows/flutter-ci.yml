name: Flutter CI

on:
  push:
    branches:
      - feature/expense-tracker-ci-cd

jobs:
  hello:
    runs-on: ubuntu-latest

    steps:
      - name: Print Hello
        run: echo "Hello Flutter CI!"
